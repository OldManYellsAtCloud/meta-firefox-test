header:
  version: 11
  includes:
    - kas/ff/latest.yml
    - kas/repos/scarthgap.yml
    - kas/common.yml

machine: qemuarm64

local_conf_header:
  image_name: |
    IMAGE_NAME = "glibc-scarthgap-latest-aarch64"
    BBMASK += "meta-lts-mixins/recipes-devtools/rust"
