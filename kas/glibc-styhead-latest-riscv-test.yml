header:
  version: 11
  includes:
    - kas/ff/latest.yml
    - kas/repos/styhead.yml
    - kas/common.yml

machine: qemuriscv64

local_conf_header:
  image_name: |
    IMAGE_NAME = "glibc-styhead-latest-riscv"
    BBMASK += "meta-lts-mixins/recipes-devtools/rust"
