header:
  version: 11
  includes:
    - kas/ff/latest.yml
    - kas/repos/whinlatter.yml
    - kas/common.yml

machine: qemuarm64

local_conf_header:
  image_name: |
    IMAGE_NAME = "glibc-whinlatter-latest-aarch64"
